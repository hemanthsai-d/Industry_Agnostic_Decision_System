[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -p no:cacheprovider -m "not integration"
markers =
    integration: tests that require external services (Redis/Postgres/etc.)
